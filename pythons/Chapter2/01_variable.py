
# these are variables and they acts like conatiner which stores some sort of data in them like string integer floating etc.
# they are basically memory address inside the ram which is allocated by the user when he declares a variable on code execution

a =1
b=2
c = 7

name = "kachua"  # double " " code means string and individaul letter without "" eg. Kachua works as and identifier and it will throw an error
# manes = kachua       :::: "kachua" is not defined  aisa ek error because currently kachua here as an identifier read kiya jaa raha hai not as a string because string is identified by ""
print(a+b)